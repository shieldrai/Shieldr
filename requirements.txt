# Shieldr — Python Dependencies
# Install with: pip install -r requirements.txt

# ---------------------------------------------------------------------------
# Web3 / Blockchain
# ---------------------------------------------------------------------------
web3>=6.15.0                  # EVM RPC client
eth-abi>=4.2.0                # ABI encoding/decoding
eth-account>=0.11.0           # Transaction & signature utilities
eth-utils>=3.0.0              # Address checksumming, hex utilities

# ---------------------------------------------------------------------------
# HTTP & Networking
# ---------------------------------------------------------------------------
httpx>=0.27.0                 # Async-capable HTTP client (threat-intel feeds)
aiohttp>=3.9.0                # Async HTTP (used in simulation module)
requests>=2.31.0              # Synchronous HTTP fallback

# ---------------------------------------------------------------------------
# Data Handling & Parsing
# ---------------------------------------------------------------------------
pydantic>=2.6.0               # Data validation and settings management
PyYAML>=6.0.1                 # YAML config loading
python-dotenv>=1.0.0          # .env environment variable loader

# ---------------------------------------------------------------------------
# Security & Cryptography
# ---------------------------------------------------------------------------
cryptography>=42.0.0          # Low-level crypto primitives
pyOpenSSL>=24.0.0             # TLS/SSL utilities (PhishNet URL checks)
certifi>=2024.2.2             # CA bundle for HTTPS verification

# ---------------------------------------------------------------------------
# Analysis & Intelligence
# ---------------------------------------------------------------------------
# (Add AI/ML packages here when the AI scoring layer is implemented)
# openai>=1.0.0               # Optional: GPT-based contract explanation

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
click>=8.1.7                  # CLI argument parsing (dev helpers)
rich>=13.7.0                  # Rich terminal output for self-test
tenacity>=8.2.3               # Retry logic for RPC and API calls
cachetools>=5.3.3             # In-memory cache for risk results

# ---------------------------------------------------------------------------
# Testing & Development
# ---------------------------------------------------------------------------
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
responses>=0.25.0             # HTTP mock for tests
black>=24.3.0                 # Code formatter
isort>=5.13.2                 # Import sorter
pylint>=3.1.0                 # Linter
