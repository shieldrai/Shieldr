# Shieldr — Python dependencies
#
# Production runtime: zero external dependencies.
# guard.py uses only the Python standard library:
#   base64, math, re, sys, unicodedata, dataclasses, typing, argparse
#
# The packages below are for development and testing only.

# Testing
pytest>=8.1.0
pytest-cov>=5.0.0

# Code quality
black>=24.3.0
isort>=5.13.2
