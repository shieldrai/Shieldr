# Shieldr — Python dependencies
#
# guard.py uses only the Python standard library for all core detection:
#   base64, binascii, math, re, sys, unicodedata, dataclasses, typing
#
# No third-party runtime dependencies are required for the core skill.

# ---------------------------------------------------------------------------
# Testing & Development
# ---------------------------------------------------------------------------
pytest>=8.1.0
pytest-cov>=5.0.0
black>=24.3.0
isort>=5.13.2
